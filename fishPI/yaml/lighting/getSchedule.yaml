Get the current schedule for a given channel
    ---
    tags:
      - Lighting
    parameters:
      - name: channel
        in: query
        type: integer
        required: true
        description: Channel Number