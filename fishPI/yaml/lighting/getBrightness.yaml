Get Brightness of a given light channel
    ---
    tags:
      - Lighting
    parameters:
      - name: channel
        in: query
        type: integer
        required: true
        description: Channel Number