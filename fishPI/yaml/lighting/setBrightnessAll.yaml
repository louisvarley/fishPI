Set the brightness for all channels
    ---
    tags:
      - Lighting
    parameters:
      - name: percentage
        in: query
        type: integer
        required: true
        description: Light Brightness Percentage