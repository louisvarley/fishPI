import logging
import fishPI
import fishPI.config

def logDebug(str):
    #logging.basicConfig(filename='fishpi.log',level=logging.DEBUG)
    #logging.debug(str)
    print(str)

def logInfo(str):
    #logging.basicConfig(filename='fishpi.log',level=logging.INFO)
    #logging.info(str)
    print(str)

def logWarning(str):
    #logging.basicConfig(filename='fishpi.log',level=logging.WARNING)
    #logging.warning(str)
    print(str)


